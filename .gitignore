*.iml
.gradle
.kotlin
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
# Records how one developer sorted the device table. Same class as workspace.xml.
/.idea/deviceManager.xml
# Android Studio sometimes drops an .idea/ inside a module. That is per-user
# state and never belongs in the repo; only the root .idea/ is partly shared.
/*/.idea/
.DS_Store
# Unanchored so it also covers :core, :data and :domain, not just the root.
# Anchored "/build" let module build output reach the index.
build/
/captures
.externalNativeBuild
.cxx

# Holds the Supabase URL, publishable key and database password.
local.properties
